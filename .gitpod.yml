image:
  file: .gitpod.Dockerfile

tasks:
  - before: printf "\n[settings]\napi_key = $WAKA_TIME_API_KEY\n" > ~/.wakatime.cfg && chmod +x dotnet.sh && ./dotnet.sh
    

vscode:
  extensions:
    - muhammad-sammy.csharp